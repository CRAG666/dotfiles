#!/bin/bash
# pinentry-rofi (AUR) reemplazado por pinentry-qt (oficial, qt6-base ya instalado).
# Se pierde el tema rofi: no hay pinentry con UI rofi en repos oficiales.
exec /usr/bin/pinentry-qt "$@"
