# vim: ft=swayconfig
# Colores del tema eyes para scroll. Solo este include conmuta entre modos
# (via el symlink colors.conf); el resto de la config vive en ./config,
# trackeada y editable directo. La variante dark se regenera con
# `eyes-theme generate` — edita solo la light (colors.eyes.conf).

client.focused #c87868 #536053 #d0dcc8 #b890b0 #c87868
client.focused_inactive #3a463a #2d382d #d0dcc8 #b890b0 #3a463a
client.unfocused #3a463a #1a211a #d0dcc8 #b890b0 #3a463a

jump_labels_color #c87868FF
