# █▄▀ █▀▀ █▄█ █▄▄ █ █▄░█ █▀▄
# █░█ ██▄ ░█░ █▄█ █ █░▀█ █▄▀

#### Definición de flags de enlace ####
# l -> bloqueado (funciona incluso con inhibidor de entrada activo, p. ej. pantalla de bloqueo)
# r -> liberar (al soltar la tecla)
# o -> pulsación larga (tras tiempo prolongado)
# e -> repetir (mientras se mantenga presionada)
# n -> no consume (se propaga evento a la ventana activa)
# m -> ratón (úsese con submapas de mouse)
# t -> transparente (no puede ser ensombrecido por otros enlaces)
# i -> ignorar mods (ignora modificadores)
# s -> separado (combina arbitrariamente mods/teclas; ver Combos de teclas)
# p -> omite solicitudes de la app para inhibir combinaciones de teclas

#### Variables de entorno ####
$ROFI_SCRIPTS=~/.config/rofi/run.sh
$DUNST_SCRIPTS=~/.config/dunst/
$notifycmd=notify-send -h string:x-canonical-private-synchronous:hypr-cfg -u low
$RUN_TUI=kitty --class dialog

#### Mouse ####
bindd = SUPER, Space, Mover cursor, exec, wl-kbptr && hyprctl dispatch submap reset
bind  = SUPER, Space, submap, cursor
submap= cursor
  bind = , Escape, submap, reset
submap= reset

#### Atajos de aplicación ####
bindd = ALT,   Space,      Screen lock,    exec, swaylock
bindd = SUPER, Backspace, Open powermenu, exec, pkill rofi || $ROFI_SCRIPTS powermenu

bindd = SUPER, T, Open terminal,      exec, kitty
bindd = SUPER, V, Open clipboard,     exec, pkill rofi || $ROFI_SCRIPTS clipboard
bindd = ,      XF86Launch6, Open clipboard, exec, pkill rofi || $ROFI_SCRIPTS clipboard
bind  = ,      XF86Mail,   exec, betterbird
bindd = ,      XF86Search, Open Web Browser, exec, zen-browser

bindd = , XF86Launch5, FullScreen launcher, exec, pkill rofi || $ROFI_SCRIPTS launcher
bindd = , XF86Open,    Open drun,          exec, pkill rofi || $ROFI_SCRIPTS menu drun
bindd = , XF86Launch7, Open run menu,      exec, pkill rofi || $ROFI_SCRIPTS menu run
bindd = , Find,        Open active apps,   exec, pkill rofi || $ROFI_SCRIPTS menu window
bindd = , XF86Tools,   Open tools,         exec, pkill rofi || $ROFI_SCRIPTS apps
bindd = , XF86Favorites, Open bookmarks,   exec, pkill rofi || $ROFI_SCRIPTS bookmarks
bindd = SUPER, B, Open bookmarks quick links, exec, pkill rofi || $ROFI_SCRIPTS quicklinks
bindd = , XF86Go,       Open wifi menu, exec, $RUN_TUI -T impala impala
bindd = , XF86Explorer, Open File Manager, exec, $RUN_TUI -T yazi yazi
bindd = SUPER, C, Show credentials,    exec, pkill rofi || $ROFI_SCRIPTS passman
bindd = SUPER, P, Open prompt gallery, exec, pkill rofi || $ROFI_SCRIPTS prompt_gallery

#### Controles multimedia ####
# Volumen
binde = , XF86AudioRaiseVolume, exec, $DUNST_SCRIPTS/vol up
binde = , XF86AudioLowerVolume, exec, $DUNST_SCRIPTS/vol down
bind  = , XF86AudioMute,        exec, $DUNST_SCRIPTS/vol mute
bind  = , XF86AudioPreset,      exec, pwvucontrol

# Reproducción
bindl = , XF86AudioPrev,     exec, playerctl previous
bindl = , XF86AudioNext,     exec, playerctl next
bindl = , XF86AudioPlay,     exec, playerctl --all-players play-pause
bindl = , XF86AudioMicMute,  exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

#### Capturas de pantalla ####
bind = SUPER, Print, exec, grimblast --notify --freeze copysave screen
bind = ,      Print, exec, grimblast --notify --freeze copysave area

#### Scripts personalizados ####
bind = SUPERALT, W, exec, pkill rofi || ~/.config/awww/awwwallselect.sh  # cambiar wallpaper

#### Espacios de trabajo especiales ####
bind = SUPER,      A, togglespecialworkspace
bind = SUPERSHIFT, A, movetoworkspace, special
bind = SUPER,      G, togglespecialworkspace, AI

bind = SUPER, semicolon, focusmonitor, +1
bind = SUPERSHIFT, semicolon, movewindow, mon:+1

#### Modo de redimensionamiento ####
bind  = SUPER, R, submap, resize
bind  = SUPER, R, exec, $notifycmd 'Resize Mode'
submap = resize
  binde = , l, resizeactive,  100  0
  binde = , h, resizeactive,  -100 0
  binde = , k, resizeactive,   0  -100
  binde = , j, resizeactive,   0   100
bind  = , Escape, submap, reset
submap = reset

#### Atajos de ratón ####
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, Control_L,    movewindow
bindm = SUPER, mouse:273, resizewindow
bindm = SUPER, ALT_L,       resizewindow

#### Minimizar ventana ####
bind = SUPERSHIFT, S, togglespecialworkspace, magic
bind = SUPERSHIFT, S, movetoworkspace, +0
bind = SUPERSHIFT, S, togglespecialworkspace, magic
bind = SUPERSHIFT, S, movetoworkspace, special:magic
bind = SUPERSHIFT, S, togglespecialworkspace, magic

bind = SUPERCTRL, F, togglefloating,

bind = SUPERSHIFT,F,fullscreen,1
bind = SUPER, F, fullscreen, # Fullscreen Mode

# Floating Mode
bind = SUPERALT, F, workspaceopt, allfloat
bind = SUPERALT, F, exec, $notifycmd 'Toggled All Float Mode'

bind = SUPER, Q, killactive
