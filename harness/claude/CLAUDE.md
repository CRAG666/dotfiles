# Code generation

The directives imported below are always in effect, in every project, without being invoked.
They are the default; an explicit instruction from me in the conversation overrides them.

@/home/think-crag/.claude/skills/code-style-defaults/SKILL.md
